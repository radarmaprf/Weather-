name: Weather Radar Cache Updater

on:
  schedule:
    - cron: '*/30 * * * *'   # каждые 30 минут
  workflow_dispatch:          # ручной запуск

permissions:
  contents: write             # разрешаем пушить в репозиторий

jobs:
  update-cache:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Update weather cache
      run: |
        python update.py   # ← теперь запускается скрипт, который завершается
    
    - name: Commit and push cache
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add cache.json
        git diff --staged --quiet || (git commit -m "Обновление кеша погоды [skip ci]" && git push)
